<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="hidden">
    <%=  f.number_field :book_id , value: @book.id %>
  </div>

  <div class="field">
    <%= f.label :"Номер страницы" %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :Текст %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit "Добавить" %>
  </div>
<% end %>


<script type="text/javascript">
    $(window).ready(function(){
        $(".hidden").hide();
    });
</script>
