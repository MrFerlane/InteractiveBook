<h1>Мои книги</h1>

<table>
  <thead>
    <tr>
      <th>Автор</th>
      <th>Название</th>
      <th>Описание</th>
      <th>Статус</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <% if book.user_id ==
      <tr>
        <td><%= User.wbook.user_id %></td>
        <td><%= book.name %></td>
        <td><%= book.description %></td>
        <td><%= book.status %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
